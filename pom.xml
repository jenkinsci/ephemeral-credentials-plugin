<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.jenkins-ci.plugins</groupId>
    <artifactId>plugin</artifactId>
    <version>6.2211.v27f680c93c53</version>
    <relativePath />
  </parent>

  <groupId>com.myprovys.ci</groupId>
  <artifactId>ephemeral-credentials-provider</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>hpi</packaging>

  <name>Ephemeral Credentials Provider</name>
  <description>Run-scoped, in-memory CredentialsProvider: resolves credentials only for the
        Pipeline build currently executing, and only what that build itself cached.
        Nothing it holds is ever written to disk.</description>

  <properties>
    <!-- Initially aligned with the core version that our internal JSL
             currently targets. Probably best to bump both together.
      -->
    <jenkins.version>2.462.3</jenkins.version>
    <java.level>11</java.level>

    <!-- Parent POM's BannedDependencies rule requires this exact property
             name rather than a literal <version> on the dependency itself.
             Re-confirmed still necessary (2026-07): the parent POM's own
             default (2573.vd91b_8a_43e019 as of this check) doesn't match
             our jenkins.version and reproduces the exact NoSuchMethodError
             this override was originally added to fix. -->
    <jenkins-test-harness.version>2207.v3b_df04c801d4</jenkins-test-harness.version>

    <!-- The parent POM already declares spotless-maven-plugin (Palantir Java
             Format, plus sortPom for pom.xml) bound to `check` at `verify` -
             gated behind this property, off by default ("Set to false to
             enable Spotless" per the parent's own comment). Flipping it here
             is all that's needed; adding a second, separate <plugin>
             declaration would just duplicate the inherited one (and risk
             configuring the wrong formatter). -->
    <spotless.check.skip>false</spotless.check.skip>
  </properties>

  <!-- Several of our dependencies (workflow-cps / pipeline-input-step /
         lockable-resources) each transitively want their own versions of
         workflow-support, script-security, workflow-api, etc., which trips
         the parent POM's RequireUpperBoundDeps enforcer rule if resolved
         piecemeal. Importing the BOM for this exact core line gives one
         mutually-consistent version set instead of pinning each conflict by
         hand as it's discovered - so most dependencies below omit <version>
         and let the BOM supply it. -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.jenkins.tools.bom</groupId>
        <artifactId>bom-2.462.x</artifactId>
        <version>3387.v0f2773fa_3200</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- git 5.5.2 (needed for WithEphemeralCredentialsCustomLibraryTest)
               requires ephemeral_credentials >= 1378.v81ef4269d764, newer than what
               this BOM snapshot provides (1371.vfee6b_095f0a_3) - overridden
               here rather than bumping the whole BOM. Verified safe: this is
               the exact version this project originally pinned before
               switching to BOM management, and predates the FormException
               change on UsernamePasswordCredentialsImpl's constructor that
               only appeared in a much later ephemeral_credentials release (see
               EphemeralUsernamePassword's defensive catch for that). -->
      <dependency>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>credentials</artifactId>
        <version>1378.v81ef4269d764</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <!-- Community plugin, not part of the Jenkins-org BOM - version
                 verified against internal JSL build.gradle.kts, which
                 already pins this exact release for the same core baseline.
        -->
      <groupId>org.6wind.jenkins</groupId>
      <artifactId>lockable-resources</artifactId>
      <version>1315.v4ea_8e5159ec8</version>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>credentials</artifactId>
    </dependency>

    <!-- Real (non-test) dependencies: WithEphemeralCredentials.groovy calls
             the `lock` and `input` steps by name, so these must be declared
             here to end up in the plugin's manifest as required dependencies -
             Jenkins should refuse to load this plugin without them installed,
             same as it would for any other step it composes with. Neither is
             referenced from Java, so no compile-time import needed for them. -->
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>pipeline-input-step</artifactId>
    </dependency>

    <!-- For EphemeralSecretText's StringCredentialsImpl and
             EphemeralSecretFile's FileCredentialsImpl. -->
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>plain-credentials</artifactId>
    </dependency>

    <!-- For EphemeralSSHUserPrivateKey's BasicSSHUserPrivateKey. Optional:
             installations that never use ephemeralSSHUserPrivateKey don't
             need this plugin installed at all - EphemeralSSHUserPrivateKey's
             constructor detects its absence and fails with a clear message
             instead. Marking it optional here makes maven-hpi-plugin record
             it as an optional plugin dependency in the manifest, so Jenkins
             doesn't refuse to load this whole plugin without it. -->
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>ssh-credentials</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.jenkins-ci.plugins.workflow</groupId>
      <artifactId>workflow-cps</artifactId>
    </dependency>

    <!-- Test-only: WithEphemeralCredentialsCustomLibraryTest, illustrating
             a custom credential type defined in a shared library rather than
             in this plugin. Verified against
             https://updates.jenkins.io/download/plugins/pipeline-groovy-lib/
             (2024-10-11 release, requires core 2.414.3, compatible with our
             2.462.3 baseline) and .../plugins/git/ (2024-10-04, requires
             2.440.3). -->
    <dependency>
      <groupId>io.jenkins.plugins</groupId>
      <artifactId>pipeline-groovy-lib</artifactId>
      <version>740.va_2701257fe8d</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.jenkins-ci.main</groupId>
      <artifactId>jenkins-test-harness</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Test-only: declarative pipeline + JenkinsRule integration tests. -->
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>credentials-binding</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>git</artifactId>
      <version>5.5.2</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <!-- GitSampleRepoRule - a local, throwaway git repo to load the
                 test shared library from, ships in the git plugin's own
                 test-jar. -->
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>git</artifactId>
      <version>5.5.2</version>
      <classifier>tests</classifier>
      <scope>test</scope>
    </dependency>
    <dependency>
      <!-- GitSampleRepoRule's own superclass (AbstractSampleDVCSRepoRule,
                 which actually declares write()/mkdirs()) lives here. -->
      <groupId>org.jenkins-ci.plugins</groupId>
      <artifactId>scm-api</artifactId>
      <classifier>tests</classifier>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins.workflow</groupId>
      <artifactId>workflow-basic-steps</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins.workflow</groupId>
      <artifactId>workflow-durable-task-step</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins.workflow</groupId>
      <artifactId>workflow-job</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkins-ci.plugins.workflow</groupId>
      <artifactId>workflow-step-api</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- Declarative pipeline (`pipeline { ... }`) support. -->
    <dependency>
      <groupId>org.jenkinsci.plugins</groupId>
      <artifactId>pipeline-model-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <!-- Not managed by the Jenkins BOM even at its latest snapshot for
                 this core line (checked 2026-07) - a small, rarely-updated
                 plugin that appears to never have been added to BOM
                 coverage. Pinned by hand, matching internal JSL. -->
      <groupId>org.jenkinsci.plugins</groupId>
      <artifactId>pipeline-model-declarative-agent</artifactId>
      <version>1.1.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkinsci.plugins</groupId>
      <artifactId>pipeline-model-definition</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jenkinsci.plugins</groupId>
      <artifactId>pipeline-model-extensions</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- GitSampleRepoRule is a classic JUnit 4 @Rule (matching how the
             pipeline-groovy-lib plugin's own test suite tests this exact
             feature) - the vintage engine lets our JUnit-Platform-based
             surefire run that one test class alongside the rest, which are
             JUnit 5. Version matched to the junit-jupiter version already
             resolved transitively via jenkins-test-harness. -->
    <dependency>
      <groupId>org.junit.vintage</groupId>
      <artifactId>junit-vintage-engine</artifactId>
      <version>6.1.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <repositories>
    <repository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </repository>
  </repositories>
  <pluginRepositories>
    <pluginRepository>
      <id>repo.jenkins-ci.org</id>
      <url>https://repo.jenkins-ci.org/public/</url>
    </pluginRepository>
  </pluginRepositories>

  <build>
    <plugins>
      <!-- We have real tests now (WithEphemeralCredentialsTest); the
                 parent POM's auto-generated JenkinsRule smoke test
                 (InjectedTest) references a org.jvnet.hudson.test.injected
                 package this jenkins-test-harness version doesn't have. -->
      <plugin>
        <groupId>org.jenkins-ci.tools</groupId>
        <artifactId>maven-hpi-plugin</artifactId>
        <configuration>
          <disabledTestInjection>true</disabledTestInjection>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
